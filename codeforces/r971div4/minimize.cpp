#include <bits/stdc++.h>
#define fastio ios_base::sync_with_stdio(false); cin.tie(nullptr);
#define endl "\n"
using namespace std;

int main(){
    fastio
    int t;
    cin >> t;

    while (t--){
        // code here
        int a, b;
        cin >> a >> b;
        cout << b-a << endl;
        // int sum = INT32_MAX;
        // cin >> a >> b;

        // for (int i = a; i <= b; i++){
        //     sum = min(sum, (i-a)+(b-i));
        // }
        // cout << sum << endl;
        
    }
}